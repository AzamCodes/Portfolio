/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "assets.aceternity.com",
      "unsplash.com",
      "images.unsplash.com",
      "api.microlink.io",
    ], // Added images.unsplash.com
  },
};

export default nextConfig;
